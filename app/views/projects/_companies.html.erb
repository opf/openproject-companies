<% if @project.companies.size >= 1 %>
  <h3><%= t(:label_company_plural) %></h3>
  <ul>
    <% @project.all_companies.each do |company| %>
      <li><%= link_to company.name, company %></li>
    <% end %>
  </ul>
<% end %>
